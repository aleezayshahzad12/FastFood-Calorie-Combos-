import React from 'react';
import ReactDOM from 'react-dom/client';
import CalorieFront from './CalorieFront'; 

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <CalorieFront />
  </React.StrictMode>
);
